# React-powered table project

## Installation
Open your OS CLI(bash/Powershell etc.) in current folder and install dependencies:
```bash
npm i
```

## Starting project
Open your OS CLI(bash/Powershell etc.) in current folder and run:
```bash
npm run dev
```